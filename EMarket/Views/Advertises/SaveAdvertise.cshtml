@using EMarket.Core.Application.ViewModels.Advertises;

@model SaveAdvertisesViewModel
@{
    bool editMode = Model.Id == 0 ? false : true;
}

<div class="col-12 d-flex justify-content-center">
    <div class="col-5">
        @if (Model.Categories == null || Model.Categories.Count == 0)
        {
            <div class="card-header bg-dark d-flex justify-content-center align-items-center">
                <h4 class="text-white">No hay categorias disponibles, no se puede crear el anuncio</h4>
            </div>
            <a asp-action="Index" asp-controller="Advertises" class="btn btn-secondary d-flex justify-content-center">Volver atras</a>
        }
        else
        {
            <div class="card-header bg-dark d-flex justify-content-center align-items-center">
                <h4 class="text-white">@(editMode ? $"Editando anuncio: {Model.ProductName}" : "Creando nuevo anuncio")</h4>
            </div>
            <div class="card-body">
                <form method="post" enctype="multipart/form-data" asp-controller="Advertises" asp-action="@(editMode ? "Edit" : "Create")" class="d-flex flex-column align-items-center">

                    <div class="col-9 mb-3">
                        <label asp-for="ProductName" class="form-label">Nombre</label>
                        <input class="form-control" asp-for="ProductName">
                        <span asp-validation-for="ProductName" class="text-danger"></span>
                    </div>

                    <div class="col-9 mb-3">
                        <label asp-for="Description" class="form-label">Descripcion</label>
                        <textarea class="form-control" asp-for="Description"></textarea>
                        <span asp-validation-for="Description" class="text-danger"></span>
                    </div>

                    
                        <div class="col-9 mb-3">
                        <label asp-for="File1" class="form-label">Imagen</label>
                            @if (editMode)
                            {
                            <input asp-for="File1" class="form-control">
                            }
                            else
                            {
                            <input required asp-for="File1" class="form-control">
                            }
                        <span asp-validation-for="File1" class="text-danger"></span>
                        </div>

                    <div class="col-9 mb-3">
                        <label asp-for="File2" class="form-label">Imagen</label>
                        @if (editMode)
                        {
                            <input asp-for="File2" class="form-control">
                        }
                        else
                        {
                            <input asp-for="File2" class="form-control">
                        }
                        <span asp-validation-for="File2" class="text-danger"></span>
                    </div>
                    <div class="col-9 mb-3">
                        <label asp-for="File3" class="form-label">Imagen</label>
                        @if (editMode)
                        {
                            <input asp-for="File3" class="form-control">
                        }
                        else
                        {
                            <input asp-for="File3" class="form-control">
                        }
                        <span asp-validation-for="File3" class="text-danger"></span>
                    </div>
                    

                    <div class="col-9 mb-3">
                        <label asp-for="Price" class="form-label">Precio</label>
                        <input type="number" min="0" step="0.25" class="form-control" asp-for="Price">
                        <span asp-validation-for="Price" class="text-danger"></span>
                    </div>

                    <div class="col-9 mb-3">
                        <label asp-for="CategoryId" class="form-label">Categoria del producto</label>
                        <select asp-for="CategoryId" asp-items="@(new SelectList(Model.Categories, "Id", "Name"))" 
                            class="form-select">
                            <option value="0" selected>Selecciona una categoria</option>
                        </select>
                        <span asp-validation-for="CategoryId" class="text-danger"></span>
                    </div>

                    <input type="number" type="hidden" class="form-control" asp-for="Id">

                    <div class="col-9 d-flex justify-content-between">
                        <a asp-action="Index" asp-controller="Advertises" class="btn btn-secondary">Volver atras</a>
                        <button class="btn btn-success">Guardar</button>
                    </div>
                </form>
            </div>
        }
    </div>
</div>